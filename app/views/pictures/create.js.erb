<% if @picture.new_record? %>
  alert("Failed to upload picture: <%= j @picture.errors.full_messages.join(',').html %>");
<% else %>
  $("#pictures").prepend("<%= j render(@picture) %>");
  $("#picture").prepend("<%= j render(@picture) %>");
<% end %>
